---
# MEMSHADOW Secrets Template
# Classification: UNCLASSIFIED
# IMPORTANT: This is a TEMPLATE - DO NOT commit actual secrets to git
# Use kubectl create secret or sealed-secrets for production

apiVersion: v1
kind: Secret
metadata:
  name: memshadow-secrets
  namespace: memshadow
  labels:
    app.kubernetes.io/name: memshadow
    app.kubernetes.io/component: configuration
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values with actual secrets
  # Use: kubectl create secret generic memshadow-secrets --from-literal=key=value

  # Database Credentials
  POSTGRES_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  DATABASE_URL: "postgresql://memshadow:REPLACE_WITH_SECURE_PASSWORD@postgres-service:5432/memshadow"

  # Redis Credentials
  REDIS_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"

  # ChromaDB Credentials
  CHROMA_TOKEN: "REPLACE_WITH_SECURE_TOKEN"

  # Application Secrets
  SECRET_KEY: "REPLACE_WITH_SECURE_SECRET_KEY"
  JWT_SECRET_KEY: "REPLACE_WITH_SECURE_JWT_SECRET"

  # LureCraft SMTP (if used)
  LURECRAFT_SMTP_USER: ""
  LURECRAFT_SMTP_PASSWORD: ""

  # C2 TLS Certificates (base64 encoded)
  # C2_TLS_CERT: "base64_encoded_cert"
  # C2_TLS_KEY: "base64_encoded_key"

# ---
# Example of creating secrets securely:
# kubectl create secret generic memshadow-secrets \
#   --from-literal=POSTGRES_PASSWORD=$(openssl rand -base64 32) \
#   --from-literal=REDIS_PASSWORD=$(openssl rand -base64 32) \
#   --from-literal=CHROMA_TOKEN=$(openssl rand -base64 32) \
#   --from-literal=SECRET_KEY=$(openssl rand -base64 32) \
#   --from-literal=JWT_SECRET_KEY=$(openssl rand -base64 32) \
#   --namespace memshadow
